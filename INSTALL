Crank System: A Library for gaming.
******************************************

1. autotools setting

This package is built with autotools. 

This package does not provides autogen.sh, so use autoreconf.


2. General Installation

If it is obtained from tarball, then it comes with configure script. Then you
can install by below.

$ ./configure --prefix=/usr ...
$ make -j4
$ sudo make install
